<Window x:Class="ATLASPlotterJSON.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ATLASPlotterJSON"
        mc:Ignorable="d"
        Title="Atlas Plotter" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Top Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button x:Name="btnLoadImage" Content="Load Image" Padding="8,3" Margin="0,0,10,0" Click="btnLoadImage_Click"/>
            <Button x:Name="btnSaveAtlas" Content="Save Atlas JSON" Padding="8,3" Margin="0,0,10,0" Click="btnSaveAtlas_Click" IsEnabled="False"/>
            <Button x:Name="btnClearSelections" Content="Clear All" Padding="8,3" Margin="0,0,10,0" Click="btnClearSelections_Click" IsEnabled="False"/>
            <Separator Width="15" Opacity="0"/>
            <Button x:Name="btnZoomOut" Content="-" Padding="8,3" Width="25" Click="btnZoomOut_Click"/>
            <TextBlock x:Name="txtZoomLevel" Text="100%" Width="60" VerticalAlignment="Center" TextAlignment="Center"/>
            <Button x:Name="btnZoomIn" Content="+" Padding="8,3" Width="25" Click="btnZoomIn_Click"/>
            <Button x:Name="btnZoomReset" Content="Reset Zoom" Padding="8,3" Margin="5,0,0,0" Click="btnZoomReset_Click"/>
        </StackPanel>

        <!-- Image Display Area -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="10" Background="#FFE5E5E5">
            <ScrollViewer x:Name="scrollViewer" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                          PreviewMouseWheel="scrollViewer_PreviewMouseWheel">
                <Canvas x:Name="imageCanvas" Background="Transparent" 
                        MouseLeftButtonDown="imageCanvas_MouseLeftButtonDown" 
                        MouseMove="imageCanvas_MouseMove"
                        MouseLeftButtonUp="imageCanvas_MouseLeftButtonUp">
                    <Canvas.RenderTransform>
                        <ScaleTransform x:Name="zoomTransform" ScaleX="1" ScaleY="1" />
                    </Canvas.RenderTransform>
                    <Image x:Name="displayImage" RenderOptions.BitmapScalingMode="NearestNeighbor" 
                           RenderOptions.EdgeMode="Aliased" Stretch="None"/>
                </Canvas>
            </ScrollViewer>
        </Border>

        <!-- Bottom Controls -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Selection Name:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="txtSelectionName" Grid.Column="1" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <Button x:Name="btnAddSelection" Grid.Column="2" Content="Add Selection" Padding="8,3" 
                    Click="btnAddSelection_Click" IsEnabled="False"/>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Height="22">
            <TextBlock x:Name="tbSelectionInfo" />
        </StatusBar>
    </Grid>
</Window>
